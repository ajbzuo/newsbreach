# Production Environment Variables

# Application
NODE_ENV=production
PORT=5000

# Optional: Database (if using external database)
# DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Fastly CDN (for cache purging)
# FASTLY_SERVICE_ID=your_service_id
# FASTLY_API_TOKEN=your_api_token

# Optional: Monitoring (for external monitoring services)
# HEALTH_CHECK_URL=https://yourdomain.com/health